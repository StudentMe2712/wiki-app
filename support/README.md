# 📘 Локальный Wiki-сайт для внутренней сети

Простой и удобный Wiki-сайт для хранения технических данных компаний в локальной сети.

## 🎯 Возможности

- ✅ **Без авторизации** - доступ для всех пользователей в сети
- ✅ **Плоское меню** - список организаций слева
- ✅ **Редактирование** - простое редактирование текста вручную
- ✅ **Экспорт** - сохранение страниц в PDF и TXT
- ✅ **SQLite** - локальное хранение данных
- ✅ **Быстрый запуск** - один клик для старта

## 📋 Системные требования

- **ОС**: Windows 10/11 или Windows Server
- **Node.js**: версия 16.x или выше
- **RAM**: минимум 512 МБ
- **Диск**: минимум 100 МБ свободного места

## 🚀 Установка

### Шаг 1: Установка Node.js

Если Node.js еще не установлен:

1. Скачайте установщик с официального сайта: https://nodejs.org/
2. Выберите **LTS версию** (рекомендуется)
3. Запустите установщик и следуйте инструкциям
4. Проверьте установку, открыв PowerShell и выполнив:
   ```powershell
   node --version
   npm --version
   ```

### Шаг 2: Установка зависимостей проекта

1. Откройте папку проекта в проводнике
2. Дважды кликните на файл **`install.bat`**
3. Дождитесь окончания установки (появится сообщение "Установка завершена")

## ▶️ Запуск

### Вариант 1: Через bat-файл (рекомендуется)

1. Дважды кликните на файл **`start.bat`**
2. Откроется окно консоли с информацией о запуске
3. Когда увидите сообщение "Wiki-сервер запущен успешно!", откройте браузер
4. Перейдите по адресу: **http://localhost:3000**

### Вариант 2: Через командную строку

```powershell
cd путь\к\папке\wiki-app
npm start
```

## 🌐 Доступ из локальной сети

Чтобы другие компьютеры в сети могли открыть Wiki:

1. Узнайте IP-адрес компьютера с запущенным сервером:
   ```powershell
   ipconfig
   ```
   Найдите строку "IPv4-адрес", например: `192.168.1.100`

2. На других компьютерах в сети откройте браузер и введите:
   ```
   http://192.168.1.100:3000
   ```
   (замените `192.168.1.100` на ваш реальный IP-адрес)

### Важно: Настройка брандмауэра Windows

Если сайт не открывается с других компьютеров, нужно разрешить подключения:

1. Откройте **Панель управления** → **Брандмауэр Windows**
2. Нажмите **Дополнительные параметры**
3. Выберите **Правила для входящих подключений**
4. Нажмите **Создать правило**
5. Выберите **Для порта** → Далее
6. Выберите **TCP**, укажите порт **3000** → Далее
7. Выберите **Разрешить подключение** → Далее
8. Отметьте все профили → Далее
9. Введите имя: **Wiki Server** → Готово

## 📝 Использование

### Создание организации

1. Нажмите кнопку **"+ Новая wiki-страница"** в верхней панели
2. Введите название организации
3. (Опционально) Введите начальное содержимое
4. Нажмите **"Создать"**

### Редактирование страницы

1. Откройте страницу организации из левого меню
2. Нажмите кнопку **"✏️ Редактировать"**
3. Внесите изменения в текстовое поле
4. Нажмите **"💾 Сохранить"**

**Горячие клавиши:**
- `Ctrl+S` - сохранить изменения

### Экспорт страницы

1. Откройте страницу организации
2. Нажмите кнопку **"⬇️ Экспортировать"**
3. Выберите формат:
   - **📄 PDF** - для печати и отправки
   - **📝 TXT** - для редактирования в текстовом редакторе

### Удаление организации

1. Откройте страницу организации
2. Нажмите кнопку **"🗑️ Удалить"**
3. Подтвердите удаление

## 🔧 Настройка

Файл `.env` содержит основные настройки:

```env
# Порт для запуска сервера
PORT=3000

# Имя базы данных SQLite
DB_NAME=wiki.db

# Режим работы
NODE_ENV=production
```

Для изменения порта:
1. Откройте файл `.env` в любом текстовом редакторе
2. Измените значение `PORT=3000` на нужное, например `PORT=8080`
3. Сохраните файл
4. Перезапустите сервер

## 📂 Структура проекта

```
wiki-app/
├── database.js         # Работа с базой данных SQLite
├── server.js           # Основной серверный файл
├── package.json        # Зависимости проекта
├── .env                # Настройки
├── wiki.db             # База данных (создается автоматически)
├── views/              # HTML-шаблоны
│   └── index.ejs       # Главный шаблон
├── public/             # Статические файлы
│   ├── css/
│   │   └── style.css   # Стили оформления
│   └── js/
│       └── main.js     # JavaScript фронтенда
├── start.bat           # Скрипт запуска
├── install.bat         # Скрипт установки
└── README.md           # Документация
```

## 🛡️ Безопасность

⚠️ **Важно**: Этот Wiki-сайт предназначен **только для использования в локальной сети**.

- Нет системы авторизации
- Любой пользователь может создавать, редактировать и удалять страницы
- Не рекомендуется делать доступным из интернета

## 🔄 Резервное копирование

База данных хранится в файле `wiki.db`. Для создания резервной копии:

1. Остановите сервер (закройте окно консоли)
2. Скопируйте файл `wiki.db` в безопасное место
3. Запустите сервер снова

Для восстановления:
1. Остановите сервер
2. Замените файл `wiki.db` на копию
3. Запустите сервер

## 🚪 Автозапуск при включении компьютера

Чтобы Wiki запускался автоматически при старте Windows:

1. Нажмите `Win+R`, введите `shell:startup`
2. Скопируйте файл `start.bat` в открывшуюся папку
3. Теперь при каждом включении ПК Wiki будет запускаться автоматически

## ❓ Решение проблем

### Ошибка "EADDRINUSE" (порт занят)

Порт 3000 уже используется другим приложением:
- Измените порт в файле `.env`
- Или найдите и закройте приложение, использующее порт 3000

### Ошибка "Cannot find module"

Зависимости не установлены или установлены неполностью:
- Запустите `install.bat` снова
- Или выполните в командной строке: `npm install`

### Страница не открывается

1. Убедитесь, что сервер запущен (смотрите консольное окно)
2. Проверьте, что используете правильный адрес: `http://localhost:3000`
3. Попробуйте другой браузер
4. Проверьте, не блокирует ли антивирус подключение

### Не могу подключиться с другого компьютера

1. Убедитесь, что оба компьютера в одной сети
2. Проверьте настройки брандмауэра (см. раздел выше)
3. Пропингуйте сервер: `ping <IP-адрес-сервера>`

## 📞 Техническая поддержка

Если возникли проблемы или вопросы:
- Проверьте консольное окно на наличие ошибок
- Сохраните текст ошибки для диагностики
- Проверьте, установлена ли последняя версия Node.js

## 📄 Лицензия

Этот проект создан для внутреннего использования.

---

**Разработано**: 2025  
**Версия**: 1.0.0


#   w i k i - a p p  
 