# 📁 Структура проекта Wiki

## Обзор файлов и папок

```
wiki-app/
│
├── 📄 server.js                # Основной файл сервера (Express)
├── 💾 database.js              # Модуль работы с SQLite
├── 📦 package.json             # Зависимости проекта
├── 🗄️ wiki.db                  # База данных (создается при первом запуске)
│
├── ⚙️ .env                      # Настройки (порт, БД)
├── ⚙️ .npmrc                    # Настройки npm
├── 📝 .gitignore               # Игнорируемые файлы для Git
│
├── 🚀 start.bat                # Запуск сервера
├── 📦 install.bat              # Установка зависимостей
├── 🌱 seed.bat                 # Заполнение тестовыми данными
├── 🌱 seed-data.js             # Скрипт заполнения данными
│
├── 📖 README.md                # Полная документация
├── ⚡ QUICKSTART.md            # Быстрый старт
│
├── 📁 views/                   # HTML-шаблоны (EJS)
│   └── index.ejs               # Главный шаблон страницы
│
└── 📁 public/                  # Статические файлы
    ├── 📁 css/
    │   └── style.css           # Стили оформления
    └── 📁 js/
        └── main.js             # JavaScript клиентской части
```

---

## Описание ключевых файлов

### 🚀 Файлы запуска

| Файл | Назначение | Когда использовать |
|------|------------|-------------------|
| `install.bat` | Установка зависимостей | Один раз при первой установке |
| `start.bat` | Запуск сервера | Каждый раз для запуска Wiki |
| `seed.bat` | Загрузка начальных данных | Опционально, для быстрого старта |

### 💻 Серверные файлы

| Файл | Размер | Описание |
|------|--------|----------|
| `server.js` | ~8 KB | API endpoints, маршруты, экспорт PDF/TXT |
| `database.js` | ~2 KB | CRUD операции с SQLite |
| `seed-data.js` | ~2 KB | Список организаций для загрузки |

### 🎨 Файлы интерфейса

| Файл | Размер | Описание |
|------|--------|----------|
| `views/index.ejs` | ~7 KB | HTML шаблон всех страниц |
| `public/css/style.css` | ~9 KB | Стили (дизайн как на скриншотах) |
| `public/js/main.js` | ~5 KB | Логика фронтенда (AJAX, модалки) |

### 📖 Документация

| Файл | Размер | Содержание |
|------|--------|-----------|
| `README.md` | 69 KB | Полная документация с примерами |
| `QUICKSTART.md` | 2 KB | Краткая инструкция (3 шага) |
| `PROJECT_STRUCTURE.md` | Этот файл | Структура проекта |

### ⚙️ Конфигурация

| Файл | Назначение |
|------|-----------|
| `.env` | Настройки порта, БД, режима |
| `.npmrc` | Оптимизация установки пакетов |
| `.gitignore` | Исключения для Git |
| `package.json` | Зависимости и скрипты npm |

---

## База данных (wiki.db)

### Таблица: organizations

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER | ID организации (автоинкремент) |
| `name` | TEXT | Название (уникальное) |
| `content` | TEXT | Содержимое страницы |
| `created_at` | DATETIME | Дата создания |
| `updated_at` | DATETIME | Дата последнего изменения |

**Расположение**: `wiki-app/wiki.db`  
**Формат**: SQLite 3  
**Создается**: Автоматически при первом запуске

---

## API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/` | Главная страница |
| GET | `/organization/:id` | Страница организации |
| GET | `/api/organizations` | Список всех организаций (JSON) |
| GET | `/api/organizations/:id` | Получить организацию (JSON) |
| POST | `/api/organizations` | Создать организацию |
| PUT | `/api/organizations/:id` | Обновить организацию |
| DELETE | `/api/organizations/:id` | Удалить организацию |
| GET | `/api/export/pdf/:id` | Экспорт в PDF |
| GET | `/api/export/txt/:id` | Экспорт в TXT |

---

## Зависимости (package.json)

### Production

| Пакет | Версия | Назначение |
|-------|--------|-----------|
| `express` | 4.18.2 | Веб-фреймворк |
| `sqlite3` | 5.1.6 | База данных |
| `ejs` | 3.1.9 | Шаблонизатор |
| `marked` | 11.0.0 | Markdown → HTML |
| `dompurify` | 3.0.6 | Санитизация HTML |
| `puppeteer` | 21.6.0 | Генерация PDF |
| `body-parser` | 1.20.2 | Парсинг JSON/form-data |
| `dotenv` | 16.3.1 | Переменные окружения |

### Development

| Пакет | Версия | Назначение |
|-------|--------|-----------|
| `nodemon` | 3.0.2 | Автоперезагрузка при разработке |

---

## Переменные окружения (.env)

```env
PORT=3000                 # Порт сервера
DB_NAME=wiki.db           # Имя файла БД
NODE_ENV=production       # Режим работы
```

### Как изменить порт:

1. Откройте `.env` в блокноте
2. Измените `PORT=3000` на нужный
3. Сохраните и перезапустите сервер

---

## Размеры файлов

```
Общий размер: ~100 MB (с node_modules)
Без зависимостей: ~50 KB (только код проекта)

database.js          2 KB
server.js            8 KB  
views/index.ejs      7 KB
public/css/style.css 9 KB
public/js/main.js    5 KB
README.md            69 KB

wiki.db              ~20-50 KB (зависит от количества данных)
node_modules/        ~98 MB
```

---

## Команды npm

```bash
npm start           # Запуск сервера (production)
npm run dev         # Запуск с nodemon (development)
npm install         # Установка зависимостей
```

**Для Windows**: Используйте `.bat` файлы вместо npm команд!

---

## Портирование/Перенос

Для переноса на другой компьютер нужны только эти файлы:

```
✅ Обязательно:
- server.js
- database.js
- package.json
- .env
- views/
- public/
- start.bat
- install.bat

📦 Опционально (можно скопировать для ускорения):
- node_modules/      # Иначе выполните install.bat
- wiki.db            # Иначе будет пустая БД

❌ Не нужно:
- README.md, QUICKSTART.md (только документация)
- seed-data.js, seed.bat (только для первоначального заполнения)
```

---

## Разработка

### Добавление новой функции:

1. **Backend**: Редактируйте `server.js` (новые роуты) или `database.js` (новые запросы)
2. **Frontend**: Редактируйте `views/index.ejs` (HTML) или `public/js/main.js` (JS)
3. **Стили**: Редактируйте `public/css/style.css`

### Горячая перезагрузка:

```bash
npm run dev
```

При изменении файлов сервер перезапустится автоматически.

---

## Безопасность

⚠️ **Данный проект НЕ предназначен для публичного доступа!**

- Нет аутентификации
- Нет проверки прав доступа
- Только для локальной сети

**Рекомендации**:
- Не выставляйте в интернет
- Используйте только в доверенной сети
- Регулярно делайте резервные копии `wiki.db`

---

## Масштабирование

### Если нужно больше пользователей:

- ✅ До 10 пользователей: Текущая конфигурация подходит
- ⚠️ 10-50 пользователей: Рассмотрите PostgreSQL вместо SQLite
- ❌ 50+ пользователей: Нужна полноценная архитектура с авторизацией

### Если нужна авторизация:

Потребуется доработка:
- Добавить таблицу `users`
- Интегрировать passport.js или JWT
- Добавить роли (read/write)
- Обновить интерфейс (логин/логаут)

---

**Проект создан**: 1 декабря 2025  
**Версия**: 1.0.0  
**Технологии**: Node.js + Express + SQLite + EJS


